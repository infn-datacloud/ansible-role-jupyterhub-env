---
- name: Test ansible-role-jupyterhub-env
  hosts: localhost
  become: true
  vars:
    jupyterhub_dns_name: "test.cloud.infn.it"
    jupyterhub_cert_manager_type: self-signed
    jupyterhub_run: false
  roles:
    - role: ansible-role-jupyterhub-env
