---
galaxy_info:
  role_name: jupyterhub_env
  namespace: infn_datacloud
  author: Gioacchino Vino
  description: >-
    Single-node JupyterHub stack on Debian 13 with Docker Compose:
    Traefik TLS ingress, internal configurable-http-proxy, JupyterHub
    (DockerSpawner) and inline monitoring (Prometheus/cAdvisor/node-exporter/Grafana).
  company: INFN
  license: Apache-2.0
  min_ansible_version: "2.18"
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - jupyterhub
    - jupyter
    - docker
    - infn
    - cloud

# Docker is installed inline by the role (tasks/install-docker.yml): no external
# dependency. CVMFS / partitioned GPUs are scaffolded but not active.
dependencies: []
